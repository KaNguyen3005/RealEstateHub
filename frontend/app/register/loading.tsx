import { AuthPageSkeleton } from "@/components/auth/auth-page-skeleton";

export default function Loading() {
  return (
    <AuthPageSkeleton
      title="Create account"
      description="We are preparing the registration page for you."
    />
  );
}
