import { AuthPageSkeleton } from "@/components/auth/auth-page-skeleton";

export default function Loading() {
  return (
    <AuthPageSkeleton
      title="Login"
      description="We are preparing the sign-in page for you."
    />
  );
}
